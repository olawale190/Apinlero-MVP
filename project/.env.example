# ==============================================================================
# ÀPÍNLẸ̀RỌ MVP - ENVIRONMENT VARIABLES
# ==============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!
#
# SECURITY NOTICE:
# - This file should be in .gitignore
# - Use Vercel Environment Variables for production
# - Never commit real API keys or secrets
# ==============================================================================

# Supabase Configuration
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJI...your_anon_key_here

# Stripe Configuration (Get from https://dashboard.stripe.com/apikeys)
# IMPORTANT: Only the PUBLISHABLE key goes here (pk_test_... or pk_live_...)
# The SECRET key (sk_*) is stored encrypted in the database, not here!
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# Application Configuration
VITE_APP_NAME="Isha's Treat & Groceries"
VITE_APP_URL=http://localhost:5173

# Backend API URL (for Railway deployment)
VITE_API_URL=https://your-backend.railway.app

# Feature Flags
VITE_ENABLE_STRIPE_PAYMENTS=false
VITE_ENABLE_NOTIFICATIONS=true

# Email Configuration (Resend - Primary Email Service)
# Get your API key from https://resend.com/api-keys
# This is the primary email service - configure this first
VITE_RESEND_API_KEY=re_your_api_key_here
VITE_FROM_EMAIL=noreply@yourdomain.com
VITE_BUSINESS_EMAIL=info@yourdomain.com

# n8n Email Automation (Fallback - Optional)
# Get your webhook URL from n8n Cloud dashboard
# This is used as a fallback if Resend is not configured
VITE_N8N_WEBHOOK_URL=https://your-instance.app.n8n.cloud/webhook

# Anthropic Claude AI Configuration
# Get your API key from https://console.anthropic.com/settings/keys
# Used for AI-powered inventory management and promotional campaigns
ANTHROPIC_API_KEY=sk-ant-your_api_key_here

# ==============================================================================
# SUPABASE EDGE FUNCTION SECRETS (set via Supabase CLI, not here)
# ==============================================================================
# These are set using: supabase secrets set KEY=value
#
# Required secrets for Edge Functions:
# - ENCRYPTION_KEY: 32+ character string for AES-256 encryption of Stripe keys
#   Generate with: openssl rand -hex 32
#
# - STRIPE_WEBHOOK_SECRET: From Stripe Dashboard → Webhooks (whsec_...)
#
# Example:
# supabase secrets set ENCRYPTION_KEY=your-32-character-encryption-key-here
# supabase secrets set STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret
# ==============================================================================

# ==============================================================================
# PRODUCTION DEPLOYMENT CHECKLIST
# ==============================================================================
# Before deploying to production:
#
# 1. CREDENTIALS:
#    □ Replace all test credentials with production credentials
#    □ Generate new ENCRYPTION_KEY for production
#    □ Use Stripe live keys (pk_live_*, sk_live_*)
#
# 2. SECURITY:
#    □ Ensure .env is in .gitignore
#    □ Run RLS migration: supabase db push
#    □ Verify Stripe keys are encrypted in database
#    □ Set up Stripe webhooks pointing to Edge Function
#
# 3. DEPLOYMENT:
#    □ Set all VITE_* variables in Vercel dashboard
#    □ Set all secrets in Supabase dashboard/CLI
#    □ Enable HTTPS (VITE_APP_URL should use https://)
#    □ Set VITE_ENABLE_STRIPE_PAYMENTS=true when ready
#
# 4. TESTING:
#    □ Test payment processing with Stripe test cards
#    □ Verify webhook receives events
#    □ Test RLS policies (user A can't see user B's data)
# ==============================================================================

# ==============================================================================
# ÀPÍNLẸ̀RỌ MVP - ENVIRONMENT VARIABLES
# ==============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!
# ==============================================================================

# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_project_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Stripe Configuration (Get from https://dashboard.stripe.com/apikeys)
# Use test keys for development (pk_test_...) and live keys for production (pk_live_...)
VITE_STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key_here

# Application Configuration
VITE_APP_NAME="Isha's Treat & Groceries"
VITE_APP_URL=http://localhost:5173

# Backend API URL (for Railway deployment)
VITE_API_URL=https://your-backend.railway.app

# Feature Flags
VITE_ENABLE_STRIPE_PAYMENTS=false
VITE_ENABLE_NOTIFICATIONS=true

# n8n Email Automation (Get your webhook URL from n8n Cloud dashboard)
# Leave empty to disable email automation buttons in the dashboard
VITE_N8N_WEBHOOK_URL=https://your-instance.app.n8n.cloud/webhook

# ==============================================================================
# PRODUCTION DEPLOYMENT CHECKLIST
# ==============================================================================
# Before deploying to production:
# 1. Replace all test credentials with production credentials
# 2. Enable HTTPS (VITE_APP_URL should use https://)
# 3. Set VITE_ENABLE_STRIPE_PAYMENTS=true only when ready
# 4. Review Supabase RLS policies
# 5. Set up Stripe webhooks
# 6. Test payment processing thoroughly
# ==============================================================================
